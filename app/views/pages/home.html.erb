<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://www.movegb.com/s3/images/r2048x-/partner/6084_7');">
  <div class="banner-content">
    <% if user_signed_in? %>
    <h1>Welcome, <%= current_user.first_name %></h1>
    <%= link_to "#{current_user.area.name}", area_path(current_user.area), class: "btn btn-success btn-lg"  %>
  <% else %>
    <h1>Welcome- please sign in for more options</h1>
  <% end %>
  </div>
</div>

